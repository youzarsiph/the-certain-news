"""AppConf for tcn.apps.tags"""

from django.apps import AppConfig


# Create your config here.
class TagsConfig(AppConfig):
    """App configuration for tcn.apps.tags"""

    name = "tcn.apps.tags"
    default_auto_field = "django.db.models.BigAutoField"
